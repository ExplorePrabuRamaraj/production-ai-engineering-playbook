# W1D4 — Model Context Protocol (MCP) Intro
# Core dependencies

# MCP Python SDK — reference implementation for building MCP servers and clients
# Pin to 1.x; the 2024-11-05 protocol version is stable in this range
mcp>=1.0.0,<2.0.0

# OpenAI client — used in live mode to pass tool schemas to the LLM
openai>=1.30.0,<2.0.0

# Pydantic — input validation and schema enforcement in production MCP servers
pydantic>=2.0.0,<3.0.0

# JSON Schema validation — lightweight input validation for tool arguments
jsonschema>=4.0.0,<5.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0    # Required for async MCP server tests
