# No real values. Never commit .env to source control.
OPENAI_API_KEY=your-openai-api-key
MODEL=gpt-4o-mini
TEMPERATURE=0.0
MAX_TOKENS=500
DEMO_MODE=false

# Supervisor network settings
SUPERVISOR_TIMEOUT=10.0
MAX_SUBTASK_RETRIES=2

# Swarm network settings
SWARM_MAX_HOPS=5
