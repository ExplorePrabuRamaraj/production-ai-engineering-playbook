# W2D7 — Deterministic Guardrails (NeMo)
# Core dependencies — pin to compatible range, not exact version

openai>=1.30.0,<2.0.0          # LLM API client (live mode only — not needed for demo mode)
pydantic>=2.0.0,<3.0.0         # Schema validation for structured outputs

# NeMo Guardrails — full Colang runtime for production use
# The PoC implements the core concepts without requiring this dependency,
# but install it to extend the demo with real Colang policy files.
nemoguardrails>=0.9.0,<1.0.0   # NVIDIA NeMo Guardrails Colang runtime

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0         # Async test support for future extension
