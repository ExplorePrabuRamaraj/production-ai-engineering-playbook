# W3D7 — Distributed Tracing (LangSmith)
# Core LLM client
openai>=1.30.0,<2.0.0

# LangSmith tracing SDK — provides @traceable, RunTree, Client
langsmith>=0.1.0,<1.0.0

# LangChain integration (optional — enables automatic callback-based tracing
# for LangChain chains and agents without @traceable decoration)
langchain>=0.2.0,<1.0.0
langchain-openai>=0.1.0,<1.0.0

# Schema validation
pydantic>=2.0.0,<3.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
